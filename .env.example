# Directory on the host that contains planet-latest.osm.pbf and output files
# You can use an absolute or relative path.
DATA_DIR=./data

# Optional: override filenames inside /data
# PBF_IN=/data/planet-latest.osm.pbf
# PBF_HIGHWAYS=/data/planet-highways.osm.pbf
# PBF_CPP_OUT=/data/planet-filtered-cpp.osm.pbf
# PBF_OUT=/data/planet-filtered.osm.pbf
